version: '3.8'

services:
  distriflow-erp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=distriflow-secret-jwt-key-2026-super-secure
      - PORT=3000
    restart: always
